import std.canvas
use std.canvas as canvas
import std.window
use std.window as window
import std.input
use std.input as input

import types

export fn default_panel() -> Int:
    return canvas.rgb :: 34, 42, 56 :: call

export fn default_text() -> Int:
    return canvas.rgb :: 240, 240, 248 :: call






