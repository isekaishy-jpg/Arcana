import std.io
use std.io as io
import std.concurrent
use std.concurrent as concurrent
fn double(n: Int) -> Int:
    1 :: :: std.concurrent.sleep
    return n * 2

fn main() -> Int:
    let h = split double :: 21 :: call
    let x = h :: :: join
    x :: :: io.print
    return 0

